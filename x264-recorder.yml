version: '2' # Must be present exactly once at the beginning of the docker-compose.yml file

services:    # Must be present exactly once at the beginning of the docker-compose.yml file

    # Basler Camera
#    device-camera-pylon-amd64:
#        build:
#            context: https://github.com/chalmers-revere/opendlv-device-camera-pylon.git#f74da40d25dfb9ed99c2c44acfdd62f39356dcfa
#            dockerfile: Dockerfile.amd64
#        restart: on-failure
#        ipc: "host"
#        network_mode: "host"
#        volumes:
#        - /tmp:/tmp
#        command: "--camera=0 --width=640 --height=480"

#    video-x264-recorder-amd64:
#        build:
#            context: https://github.com/chalmers-revere/opendlv-video-x264-recorder.git
#            dockerfile: Dockerfile.amd64
#        restart: on-failure
#        network_mode: "host"
#        ipc: "host"
#        volumes:
#        - /tmp:/tmp
#        - /home/revere/recordings:/data
#        working_dir: /data
#        command: "--name=cam0.i420 --width=640 --height=480 --verbose"    
